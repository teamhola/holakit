{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap && cd docs && yarn",
    "build": "lerna run build",
    "build-docs": "cd docs && yarn build",
    "build-all": "yarn build && yarn build-docs"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
